// From here we are not using module.exports we will use exports instead and after that we will import not by using require keyword in script.js we will use import keyword lets see how
export const sum = (a,b)=>a+b;
export const mul = (a,b)=>a*b;
export const g = 9.8;

